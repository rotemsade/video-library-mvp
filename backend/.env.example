# --- Server ---
PORT=4000
NODE_ENV=development

# --- CORS (comma-separated origins) ---
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# --- Logging / Middleware toggles ---
LOG_LEVEL=info
REQUEST_LOGGING=1          # 1=on, 0=off
ENABLE_HELMET=1            # 1=on, 0=off

# --- API Docs ---
ENABLE_SWAGGER=0           # set to 1 when Swagger is added

# --- Feature flags / seeds ---
SEED_EPISODES=1

# --- Future: persistence (uncomment when needed) ---
# DATABASE_URL=postgres://USER:PASSWORD@localhost:5432/video_library
# REDIS_URL=redis://localhost:6379