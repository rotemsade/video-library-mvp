// frontend/index.js
// Entry file for Expo: registers your root component (App.tsx).

import { registerRootComponent } from 'expo';
import App from './App';

// Register the root component with the native runtime
registerRootComponent(App);